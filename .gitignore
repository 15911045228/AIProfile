# OS
.DS_Store
Thumbs.db

# Obsidian workspace (session state, not config)
.obsidian/workspace.json
.obsidian/workspace

# Keep Obsidian config files (core-plugins.json, app.json, etc.) as they're vault settings
